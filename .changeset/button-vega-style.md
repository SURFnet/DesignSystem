---
'@surfnet/react': patch
---

Switch the React button to the **vega** shadcn style (`base-vega`) so it matches the
Angular package, which already vendors vega. Buttons are slightly taller with a
smaller corner radius (`default` 36px, `rounded-md`) and the outline variant regains
its subtle shadow.
