---
'@surfnet/curve-react': patch
'@surfnet/curve-angular': patch
'@surfnet/curve-tokens': patch
---

Align hover and selected interaction states with Figma: item hover uses `--muted`, selected/open/active states use `--secondary`, and dark-mode `--popover` uses neutral/900 instead of stone/800.
