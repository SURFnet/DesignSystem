---
'@surfnet/curve-storybook-config': major
---

Initial release of the shared Storybook configuration package (private).

- Shared framework picker, manager UI, logo, and theme list so the React (Vite) and
  Angular (webpack) Storybooks look and behave as one system rather than two.
- Wires `@surfnet/curve-tokens` into both Storybook instances for consistent theming.
