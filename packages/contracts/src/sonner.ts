import { defineContract } from './define-contract.js';

export const sonnerContract = defineContract({
  docs: {
    description: 'A toast notification system for brief, auto-dismissing messages.',
  },
});
