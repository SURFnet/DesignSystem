import { defineContract } from './define-contract.js';

export const nativeCheckboxContract = defineContract({
  docs: {
    description:
      'A styled native <input type="checkbox">. Prefer the Base UI Checkbox for indicator icons and form-field integration.',
  },
});
