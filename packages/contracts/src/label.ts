import { defineContract } from './define-contract.js';

export const labelContract = defineContract({
  docs: {
    description: 'An accessible caption for a form control, associated via `htmlFor`.',
  },
});
