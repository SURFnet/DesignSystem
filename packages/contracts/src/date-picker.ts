import { defineContract } from './define-contract.js';

export const datePickerContract = defineContract({
  docs: {
    description: 'A calendar-backed input for picking a single date or a date range.',
  },
});
