import { defineContract } from './define-contract.js';

export const tooltipContract = defineContract({
  docs: {
    description:
      'A small popup that displays informative text when hovering or focusing an element.',
  },
});
