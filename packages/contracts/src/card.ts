import { defineContract } from './define-contract.js';

export const cardContract = defineContract({
  docs: {
    description: 'A bordered surface container with optional header, content, and footer subparts.',
  },
});
