import { defineContract } from './define-contract.js';

export const hoverCardContract = defineContract({
  docs: {
    description: 'Displays a preview of content when hovering over a trigger.',
  },
});
