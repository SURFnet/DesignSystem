import { defineContract } from './define-contract.js';

export const commandContract = defineContract({
  docs: {
    description:
      'A fast, composable command palette and searchable list for actions and navigation.',
  },
});
