import { defineContract } from './define-contract.js';

export const scrollAreaContract = defineContract({
  docs: {
    description: 'A scrollable container with a styled, cross-browser-consistent scrollbar.',
  },
});
