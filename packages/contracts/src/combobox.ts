import { defineContract } from './define-contract.js';

export const comboboxContract = defineContract({
  docs: {
    description:
      'A searchable, filterable select input combining a text field with a listbox of options.',
  },
});
