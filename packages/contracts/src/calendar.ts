import { defineContract } from './define-contract.js';

export const calendarContract = defineContract({
  docs: {
    description:
      'A date field component that allows users to enter and edit dates, or pick a date from a visual calendar.',
  },
});
