import { defineContract } from './define-contract.js';

export const dialogContract = defineContract({
  docs: {
    description:
      'A window overlaid on the page, rendering content that requires attention or actions.',
  },
});
