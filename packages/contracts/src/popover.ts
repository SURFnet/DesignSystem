import { defineContract } from './define-contract.js';

export const popoverContract = defineContract({
  docs: {
    description: 'Displays rich content in a portal, anchored to a trigger element.',
  },
});
