import { defineContract } from './define-contract.js';

export const drawerContract = defineContract({
  docs: {
    description:
      'A panel that slides in from an edge of the screen, typically for mobile-style navigation or actions.',
  },
});
