import { defineContract } from './define-contract.js';

export const navigationMenuContract = defineContract({
  docs: {
    description: 'A horizontal menu of links and flyout content for primary site navigation.',
  },
});
