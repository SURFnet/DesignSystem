import { defineContract } from './define-contract.js';

export const skeletonContract = defineContract({
  docs: {
    description: 'A placeholder that mimics the shape of content while it loads.',
  },
});
