# Angular CLI + Storybook resolve some dependencies from the root node_modules.
# Hoisting keeps their tooling working smoothly under pnpm's isolated store.
shamefully-hoist=true
auto-install-peers=true

# Pin exact versions on `pnpm add` (no ^/~ ranges) so upgrades are deliberate,
# reviewable lockfile changes.
save-exact=true
